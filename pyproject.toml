[project]
name = "pageless"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "mcp[cli]>=1.4.1",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
